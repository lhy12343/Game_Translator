<Window x:Class="GameTranslator.Gui.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:GameTranslator.Gui.ViewModels"
        xmlns:v="clr-namespace:GameTranslator.Gui.Views"
        Title="Game Translator"
        Width="960" Height="640"
        MinWidth="800" MinHeight="560"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        Background="#0F0F14">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Border BorderBrush="#2A2A38" BorderThickness="1" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="#1A1A24" CornerRadius="10,10,0,0"
                    MouseLeftButtonDown="OnDragMove">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="🎮" FontSize="16" VerticalAlignment="Center"/>
                        <TextBlock Text="Game Translator" Foreground="#E8E8F0"
                                   FontSize="13" FontWeight="SemiBold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Border Grid.Column="1" Background="Transparent"/>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,4,0">
                        <Button Content="—" Width="36" Height="28" Style="{StaticResource BtnGhost}" Click="OnMinimize"/>
                        <Button Content="✕" Width="36" Height="28" Style="{StaticResource BtnGhost}" Click="OnClose"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 主体 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 侧边导航 -->
                <Border Grid.Column="0" Background="#1A1A24" BorderBrush="#2A2A38" BorderThickness="0,0,1,0">
                    <ListBox ItemsSource="{Binding NavItems}"
                             SelectedItem="{Binding SelectedNav}"
                             Padding="8,12">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="12,10">
                                    <TextBlock Text="{Binding Icon}" FontSize="18" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Label}" FontSize="14" Margin="12,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>

                <!-- 内容区 -->
                <Border Grid.Column="1" Background="#0F0F14">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ContentControl Content="{Binding CurrentPage}" Margin="0">
                            <ContentControl.Resources>
                                <DataTemplate DataType="{x:Type vm:HomePageViewModel}">
                                    <v:HomePage/>
                                </DataTemplate>
                                <DataTemplate DataType="{x:Type vm:ApiConfigPageViewModel}">
                                    <v:ApiConfigPage/>
                                </DataTemplate>
                                <DataTemplate DataType="{x:Type vm:TranslationPageViewModel}">
                                    <v:TranslationPage/>
                                </DataTemplate>
                                <DataTemplate DataType="{x:Type vm:MonitorPageViewModel}">
                                    <v:MonitorPage/>
                                </DataTemplate>
                            </ContentControl.Resources>
                        </ContentControl>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
