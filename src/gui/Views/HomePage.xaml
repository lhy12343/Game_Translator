<UserControl x:Class="GameTranslator.Gui.Views.HomePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Padding="32,24">
    <StackPanel Margin="0,0,0,24">

        <!-- 标题 -->
        <StackPanel Margin="0,0,0,20">
            <TextBlock Text="游戏进程" FontSize="24" FontWeight="Bold" Foreground="#E8E8F0"/>
            <TextBlock Text="只显示拥有主窗口、通常位于任务栏的应用" FontSize="13" Foreground="#9090A8" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- 状态卡片 -->
        <UniformGrid Columns="4" Margin="0,0,0,20">
            <Border CornerRadius="10" Padding="16" Margin="0,0,8,0" Background="#1A1A24">
                <StackPanel>
                    <TextBlock Text="进程" FontSize="11" Foreground="#9090A8"/>
                    <TextBlock Text="{Binding SelectedProcess}" FontSize="13" FontWeight="SemiBold"
                               Foreground="#E8E8F0" TextTrimming="CharacterEllipsis" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border CornerRadius="10" Padding="16" Margin="0,0,8,0" Background="#1A1A24">
                <StackPanel>
                    <TextBlock Text="PID" FontSize="11" Foreground="#9090A8"/>
                    <TextBlock Text="{Binding SelectedProcessId}" FontSize="13" FontWeight="SemiBold"
                               Foreground="#A29BFE" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border CornerRadius="10" Padding="16" Margin="0,0,8,0" Background="#1A1A24">
                <StackPanel>
                    <TextBlock Text="状态" FontSize="11" Foreground="#9090A8"/>
                    <TextBlock Text="{Binding MonitorStatus}" FontSize="13" FontWeight="SemiBold"
                               Foreground="#00B894" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border CornerRadius="10" Padding="16" Background="#1A1A24">
                <StackPanel>
                    <TextBlock Text="窗口标题" FontSize="11" Foreground="#9090A8"/>
                    <TextBlock Text="{Binding SelectedWindowTitle}" FontSize="13" FontWeight="SemiBold"
                               Foreground="#E8E8F0" TextTrimming="CharacterEllipsis" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- 进程列表 -->
        <Border CornerRadius="10" Background="#1A1A24" Padding="4" Margin="0,0,0,20">
            <StackPanel>
                <Grid Margin="12,8,12,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="任务栏应用" FontSize="15" FontWeight="SemiBold" Foreground="#E8E8F0"/>
                    <Button Grid.Column="1" Content="🔄 刷新" Style="{StaticResource BtnGhost}"
                            Command="{Binding RefreshProcessesCommand}" FontSize="12" Padding="12,4"/>
                </Grid>
                <ListBox ItemsSource="{Binding ProcessList}"
                         SelectedItem="{Binding SelectedProcessItem}"
                         MaxHeight="180" Padding="4"/>
            </StackPanel>
        </Border>

        <!-- 操作按钮 -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Content="▶ 开始监控" Command="{Binding StartMonitoringCommand}" Style="{StaticResource BtnPrimary}"/>
            <Button Content="⏹ 停止监控" Command="{Binding StopMonitoringCommand}" Style="{StaticResource BtnSecondary}" Margin="12,0,0,0"/>
        </StackPanel>
    </StackPanel>
</UserControl>
