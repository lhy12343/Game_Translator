# 游戏实时翻译工具技术方案

> 目标：先做成稳定、可测、可维护的 Windows 单机游戏实时翻译工具，再根据真实数据扩展引擎和优化底层。
>
> 本文中的延迟和资源数字均为**验收目标**，不是未经测试的性能结论。

## 1. 产品边界

### 1.1 首版目标

- Windows 10/11。
- 优先支持 Unity Mono，再验证 Unity IL2CPP。
- 游戏文本由 XUnity.AutoTranslator 提取和写回。
- 用户配置第三方 OpenAI 兼容 API 的 URL、密钥和模型。
- 支持持久翻译缓存、基本上下文和运行指标。
- 仅面向无反作弊的单机游戏。

### 1.2 首版不做

- 不 Fork XUnity.AutoTranslator。
- 不自研共享内存无锁队列。
- 不承诺支持所有 Unity 游戏或所有游戏引擎。
- 不同时开发 Frida、MinHook、Ren'Py、RPG Maker 和 OCR。
- 不在缺少基准数据时追求“零拷贝”或固定毫秒数。

这些能力只有在真实游戏证明需要时才加入，升级条件见第 12 节。

## 2. 设计原则

1. **真实闭环优先**：先跑通一款游戏的“提取 → 翻译 → 写回”，再扩展覆盖面。
2. **复用官方扩展点**：首版使用 XUnity 内置 `CustomTranslate`，不维护上游 Fork 或自定义 Translator DLL。
3. **慢路径决定体验**：翻译 API 通常耗时数百毫秒以上，优先优化缓存、请求合并和失败恢复。
4. **游戏线程只做轻活**：截获、查本地状态和发送请求；网络、数据库和复杂解析不进入渲染线程。
5. **有界而非无限**：队列、缓存、文本长度、并发和重试都有上限。
6. **数据证明优化**：记录 P50/P95/P99、内存、CPU 和帧时间，达不到预算才升级实现。
7. **安全默认开启**：密钥不明文记录，IPC 仅限当前用户，日志默认脱敏。

## 3. 总体架构

```text
┌──────────────────────── Unity 游戏进程 ────────────────────────┐
│ XUnity.AutoTranslator                                          │
│   ├─ 官方文本提取、替换、字体与基础缓存能力                    │
│   └─ 内置 CustomTranslate Endpoint                             │
│        └─ 本机回环 HTTP GET                                    │
└───────────────────────────┬────────────────────────────────────┘
                            │ 127.0.0.1 + 随机路径令牌
┌───────────────────────────▼────────────────────────────────────┐
│ GameTranslator.exe（C# / .NET 10 / WPF）                       │
│   ├─ GUI：进程、API、语言、状态与指标                          │
│   └─ 翻译运行时                                                │
│        ├─ XUnity 本机桥接服务                                  │
│        ├─ 有界请求队列与同请求合并                             │
│        ├─ 有界内存缓存                                         │
│        ├─ SQLite 持久缓存                                      │
│        └─ 第三方翻译 API                                       │
└────────────────────────────────────────────────────────────────┘
```

首版不单独创建 `Translator.exe`。翻译运行时作为无 UI 依赖的代码运行在现有 GUI 进程内，减少部署和进程管理成本。如果 GUI 崩溃隔离或无界面运行成为真实需求，再原样抽出独立 Worker。

## 4. 技术栈

| 组件 | 选择 | 结论 |
|---|---|---|
| Windows GUI/宿主 | C#、.NET 10、WPF、CommunityToolkit.Mvvm | 保留现有实现 |
| Unity 文本链路 | XUnity.AutoTranslator 官方扩展点 | 使用，不 Fork |
| Unity Loader | 按目标游戏选择 BepInEx 或 MelonLoader | 不预设唯一最佳 |
| 首个游戏桥接 | XUnity `CustomTranslate` + 回环 HTTP | 复用内置端点，不新增游戏插件 |
| 异步排队 | `System.Threading.Channels` | .NET 原生、有界、支持取消 |
| HTTP | 单例 `HttpClient` | .NET 原生 |
| JSON | `System.Text.Json` | .NET 原生 |
| 持久存储 | SQLite（`Microsoft.Data.Sqlite`） | 翻译缓存和元数据 |
| 密钥保护 | Windows DPAPI | 首版只维护一种方案，禁止明文日志 |
| 指标 | `Stopwatch` + 结构化应用日志 | 先满足诊断，不引入遥测平台 |

### 4.1 编程语言边界

- GUI、翻译运行时和 Unity Translator 插件优先使用 C#。
- Unity 插件的目标框架必须匹配 XUnity 和目标游戏运行时，不能直接假定为 `.NET 10`。
- 只有已确认的原生函数 Hook 才引入 C/C++。
- Ren'Py 使用 Python、RPG Maker 使用 JavaScript，但都不进入首版。
- Frida 用于探索和针对性适配，不作为“任意引擎统一 Hook”。

## 5. Unity 接入

### 5.1 接入策略

1. 使用 XUnity 已内置的 `CustomTranslate`，把 `[Custom] Url` 指向软件显示的本机桥接地址。
2. XUnity 继续负责文本提取、频率保护、缓存和写回；本工具只接收文本并返回译文。
3. 只有内置端点无法提供必须的生命周期或指标时，才实现轻量 `ITranslateEndpoint` 和 Named Pipe。
4. 不修改 XUnity 的文本 Hook、缓存和写回核心。

### 5.2 Loader 选择

XUnity 官方同时支持 BepInEx、MelonLoader 等 Loader，并把 BepInEx 标为推荐项。实际选择由目标游戏决定：

- 至少准备一款 Unity Mono 测试游戏。
- 至少准备一款 Unity IL2CPP 测试游戏。
- 分别记录 Unity 版本、架构、Loader 版本、XUnity 包和安装步骤。
- 同一游戏只支持经过冒烟测试的组合，不承诺一个 Loader 覆盖全部游戏。

## 6. XUnity 本机桥接

首版直接实现 XUnity `CustomTranslate` 官方约定：

```text
GET http://127.0.0.1:52731/translate/{随机令牌}?from=ja&to=zh&text=...
响应：text/plain UTF-8 译文
```

- 仅监听 `127.0.0.1`，不接受局域网或公网连接。
- 首次启动生成 128 位随机路径令牌，避免无关本机进程误调用付费 API。
- 请求头总长度限制为 64 KiB，翻译正文仍受 20,000 字符上限约束。
- 桥接请求进入同一有界队列、缓存、超时和重试链路。
- `from`/`to` 优先使用 XUnity 本次请求值，缺失时回退到 GUI 保存的语言。
- API 密钥从不传给游戏或写入 XUnity 配置；XUnity 只保存本机桥接地址。
- GUI 退出后请求立即失败，游戏由 XUnity 保持原文，不阻塞渲染线程。

## 7. 翻译运行时

代码按具体职责放在 `src/gui/Services/Translation/`：

- `TranslationRuntime.cs`：主协调器，只管理队列、请求合并、内存缓存和指标。
- `OpenAiCompatibleClient.cs`：API 请求、响应校验、超时与有限重试。
- `TranslationDatabase.cs`：SQLite 缓存。
- `TranslationSettingsStore.cs`：普通配置与 DPAPI 密钥存取。
- `TranslationModels.cs`：配置、结果和指标数据模型。

这些模块直接组合，不为单一实现增加接口、工厂或依赖注入容器。

### 7.1 请求流程

```text
校验请求
  → 生成缓存键
  → 查有界内存缓存
  → 查 SQLite
  → 合并相同的进行中请求
  → 调用第三方 API
  → 校验响应
  → 写入 SQLite 和内存缓存
  → 返回译文
```

### 7.2 并发与背压

- 使用有界 `Channel<T>`，不允许请求无限堆积。
- 相同缓存键只保留一个进行中的 API 请求，其余调用等待同一结果。
- 固定 2 个翻译 Worker；有实测限流需求时再开放配置。
- 仅对网络错误、HTTP 429 和可恢复的 5xx 重试，并遵守 `Retry-After`。
- 重试次数有限；取消、认证失败和非法请求不重试。

### 7.3 API 兼容范围

首版支持一种明确的 OpenAI 兼容请求格式：

- API URL
- API Key
- Model
- 源语言仅支持英语、日语；目标语言固定为简体中文
- 请求超时

“OpenAI 兼容”不代表所有供应商完全一致。先完成一个经过测试的协议，供应商字段确有差异时再增加适配，不预建 Provider 工厂体系。

### 7.4 Prompt 和输出校验

- 要求模型仅返回译文或带请求 ID 的结构化结果。
- Prompt 包含目标语言、游戏语境和标记保护规则。
- 上下文从最近已确认的对话中取少量条目，并设置字符/token 上限。
- 输出缺项、重复 ID、破坏占位符或无法解析时，返回原文并记录脱敏错误。

## 8. 缓存

### 8.1 缓存层

```text
L1：进程内有界字典，处理当前会话热点
L2：SQLite，跨会话保存翻译
```

首版不自研复杂通用缓存框架。L1 先使用简单的有界淘汰策略；只有命中率或锁竞争数据证明有价值时才升级为严格 LRU。

当前 L1 按 .NET UTF-16 字符串占用估算，最多约 32 MiB；L2 按 SQLite UTF-8 文本实际字节累计，载荷最多约 224 MiB，主数据库硬上限 256 MiB。

### 8.2 缓存键

缓存正确性不能只依赖 FNV-1a Hash。缓存键至少包含：

- 完整原文
- 源语言和目标语言
- API 提供商/模型
- Prompt 版本
- 必要的游戏 ID

可使用 SHA-256 作为数据库索引，但数据库仍保存并核对完整字段，防止碰撞和错误复用。

### 8.3 SQLite 最小数据

- 翻译缓存：键字段、原文、译文、创建/最后命中时间。
- 配置元数据：Prompt 版本、Schema 版本。

数据库迁移保持向前兼容；写入使用事务，损坏或升级失败时不得覆盖原文件。

## 9. 富文本、上下文和排版

### 9.1 首版原则

- 先复用 XUnity 已有文本处理和写回能力。
- 收集真实失败样本后，再增加 Unity 专用标记保护。
- 不用一个正则声称解析 Unity、HTML、Ren'Py 和 RPG Maker 全部语法。

### 9.2 标记保护

```text
原文 → 引擎专用 tokenizer → 不可翻译 token + 可翻译片段
     → 翻译 → 校验 token 数量/顺序 → 回填
```

校验失败时返回原文，绝不把破损标记写回游戏。每种引擎拥有自己的 tokenizer 和测试样本，不共用“万能解析器”。

### 9.3 排版

优先使用 XUnity/游戏原有组件能力：自动换行、最佳适配、字号和字体替换。只有真实游戏出现溢出时，才为该文本框架实现针对性策略，并保留最小字号和可读性下限。

## 10. 安全、稳定性和合规

- API 密钥使用 Windows DPAPI 保存。
- 密钥不进入 SQLite 翻译库、崩溃日志和指标；配置文件只保存 DPAPI 密文，并与普通配置一次原子写入。数据与缓存分别位于软件目录的 `Data`、`Cache`。
- API URL 必须是合法 `https`；本机开发端点可显式允许 `localhost`。
- 网络请求设置超时、取消和最大响应体积。
- 游戏桥接仅监听回环地址，并校验随机路径令牌和请求大小。
- 日志默认脱敏原文，可由用户主动开启诊断模式。
- 游戏、桥接、API 或数据库任一环节失败时，游戏继续显示原文。
- 仅支持用户有权修改的单机游戏；对已知包含反作弊或在线竞技功能的游戏拒绝注入。
- 分发前逐项核对 XUnity、Loader、字体、模型和原生库许可证。

## 11. 性能与内存验收

下列数字是首版预算，需要在目标机器和真实游戏上测量：

| 指标 | 初始目标 |
|---|---|
| 游戏线程内的 Translator 插件工作 | P95 ≤ 0.5 ms，禁止网络/数据库同步调用 |
| 本机桥接往返（小消息，不含翻译） | P95 ≤ 5 ms |
| 缓存命中到返回译文 | P95 ≤ 16 ms |
| API 未命中 | 分开记录首字与完整响应 P50/P95/P99，不预设固定承诺 |
| 空闲 CPU | 稳定接近 0%，无轮询忙等 |
| 队列和缓存 | 始终有界，压力下不持续增长 |
| 游戏帧时间 | 同场景开启后 P95 增幅 ≤ 2% |

测试必须记录：硬件、游戏、Unity/Loader 版本、模型、网络、样本量、冷/热缓存和失败率。没有这些信息，不发布“零开销”“快一个数量级”等结论。

### 11.1 最小验证集

- 协议：截断包、粘包、超长 UTF-8、乱序响应、断线重连。
- 缓存：Hash 碰撞防护、语言/模型/Prompt 版本变化。
- 翻译：超时、取消、429、5xx、非法 JSON、标记破坏。
- 稳定性：游戏先退出、GUI 先退出、数据库只读/损坏、队列满。
- 游戏：Unity Mono 与 IL2CPP 各一款的真实闭环。
- 性能：Hook、桥接、缓存、API、帧时间分别计时，不能只测总平均值。

## 12. 升级条件

| 候选能力 | 只有满足以下条件才加入 |
|---|---|
| 独立 `Translator.exe` | GUI 生命周期确实影响翻译，或崩溃隔离/无界面运行成为需求 |
| 自定义 Endpoint + Named Pipe | `CustomTranslate` 无法满足必须的生命周期、双向指标或消息大小需求 |
| MMF/无锁队列 | Named Pipe 已被采用且在代表性负载下持续超过预算，并被证明是端到端瓶颈 |
| 严格 LRU/复杂缓存 | 简单有界缓存的命中率或锁竞争达不到指标 |
| 自研 XUnity Fork | 官方扩展点无法实现必须功能，且有可复现证据 |
| MinHook/C++ | 已知原生函数、ABI 和文本生命周期，XUnity/脚本 Hook 无法覆盖 |
| Frida | 需要快速探索未知引擎或验证 Hook 点；生产化需逐游戏评估 |
| OCR | 无法可靠取得文本，且用户接受截图权限和额外 CPU/GPU 占用 |
| 透明叠加层 | 无法写回游戏文本且 OCR 已能稳定给出位置 |
| Ren'Py/RPG Maker | 有真实目标游戏、用户需求和自动化样本 |

OCR 启动后必须独立比较 CPU、WinML 和 ONNX Runtime DirectML。DirectML 不能预设为最佳；模型转换、算子兼容、检测/识别全管线和游戏 GPU 争用都必须实测。

## 13. 实施阶段

### 阶段 0：GUI 基础（已完成）

- WPF/.NET 10 基础界面。
- 首页、API、翻译和监控页面。
- 直接启动用户选择的游戏 EXE，并按实际游戏进程采样 CPU 与工作集内存。
- 未接入游戏或翻译链路的指标显示为不可用，不用演示值冒充实测值。

### 阶段 1：真实翻译核心（代码完成，待 Windows 真实 API 实测）

1. 配置校验和安全密钥保存。
2. 单例 `HttpClient` 调用一个明确的 OpenAI 兼容 API。
3. SQLite 翻译缓存。
4. 有界队列、同请求合并、取消、超时和有限重试。
5. 用 GUI 内手动输入跑通真实翻译，并显示真实延迟/缓存指标。

**验收**：不接游戏时，输入真实文本可以稳定翻译；重启后缓存仍命中；密钥不出现在明文配置和日志中。

### 阶段 2：Unity 纵向闭环

1. 选定一款无反作弊 Unity Mono 测试游戏。
2. 使用 XUnity 内置 `CustomTranslate` 连接本机回环桥接。
3. 跑通提取、缓存/API、返回和文本写回。
4. 增加断线重连、原文降级和端到端计时。
5. 在一款 IL2CPP 游戏上验证兼容组合。

**验收**：两款测试游戏均可重复安装、启动、翻译和卸载；失败时不阻塞或崩溃游戏。

### 阶段 3：翻译质量

1. 基于失败样本补 Unity 标记保护。
2. 加入有限上下文和结构化输出校验。
3. 针对真实溢出场景调整字体与排版。

### 阶段 4：兼容矩阵与发布

1. 固化 Loader/XUnity/Unity 版本矩阵。
2. 完成安装、升级、卸载和诊断流程。
3. 做许可证、安全、杀软误报和性能回归检查。

### 阶段 5：按需求扩展

按第 12 节的升级条件选择 Ren'Py、RPG Maker、原生 Hook、Frida、OCR 或叠加层；没有真实需求的不开发。

## 14. 当前下一步

“阶段 1：真实翻译核心”和阶段 2 的 XUnity 本机桥接代码已实现。当前下一步是用户在软件中填写自己的 API 配置，并选一款无反作弊 Unity Mono 游戏完成首次真实闭环；暂不创建自定义 Endpoint、Named Pipe、MMF、C++ 注入、OCR、透明窗口或多引擎目录。

## 15. 依据

- [技术方案评审](docs/architecture-review.md)
- [XUnity.AutoTranslator 官方 README](https://github.com/bbepis/XUnity.AutoTranslator#readme)
- [XUnity.AutoTranslator 官方 README：CustomTranslate](https://github.com/bbepis/XUnity.AutoTranslator#translators)
- [.NET Memory-Mapped Files 官方文档](https://learn.microsoft.com/dotnet/standard/io/memory-mapped-files)
- [WPF 官方概述](https://learn.microsoft.com/dotnet/desktop/wpf/overview/)
- [ONNX Runtime DirectML 官方文档](https://onnxruntime.ai/docs/execution-providers/DirectML-ExecutionProvider.html)
- [SQLite 官方文档](https://www.sqlite.org/docs.html)
