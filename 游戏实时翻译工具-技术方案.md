# 游戏实时翻译工具 - 最佳性能技术方案

> **目标**：现代最强 AI 游戏实时翻译工具。有最佳轮子直接用，轮子不够最佳自己做，没轮子从零做。

---

## 0. 设计原则

1. **最佳轮子直接用**：XUnity.AutoTranslator、MelonLoader、Frida、MinHook 等成熟项目直接集成
2. **不够最佳自己做强**：翻译调度引擎、富文本处理、排版自适应、OCR 集成等自研
3. **没轮子自己做**：Renpy Hook、RPGMaker Hook、透明窗口叠加、GUI 等
4. **翻译模型走第三方 API**：用户自填密钥 + URL，不吃本地显卡
5. **每个组件用最佳语言**：不强行统一技术栈

---

## 1. 总体架构

```
┌──────────────────────────────────────────────────────────────┐
│  游戏进程                                                      │
│                                                                │
│  ┌─ Unity ──────────────────┐  ┌─ Renpy ────────┐  ┌─ 其他 ─┐│
│  │ XUnity.AutoTranslator    │  │ Python Hook    │  │ Frida  ││
│  │ + MelonLoader 注入       │  │ (monkey-patch) │  │ 统一hook││
│  │ (7种文本框架+IL2CPP)     │  │                │  │        ││
│  └───────────┬──────────────┘  └───────┬────────┘  └───┬────┘│
│         ┌────▼──────────────────────────▼────────────────▼────┐│
│         │  共享内存 (自研无锁环形队列 + Windows MMF)            ││
│         │  写入: 原文 + hash  /  读取: 译文                     ││
│         └────────────────────┬─────────────────────────────────┘│
│         ┌────────────────────▼─────────────────────────────────┐│
│         │  文本注入 (自研)                                       ││
│         │  setter替换 + 排版自适应 + 字体替换(NotoSansCJK)      ││
│         └──────────────────────────────────────────────────────┘│
└──────────────────────────────────────────────────────────────┘
                          │ 共享内存 (零拷贝)
┌─────────────────────────▼─────────────────────────────────────┐
│  翻译进程 (C# .NET 8)                                          │
│                                                                │
│  ┌─ 调度引擎 (自研) ──────────────────────────────────────────┐│
│  │  去重 -> 优先级排序 -> 100ms批量聚合 -> 超时降级              ││
│  └────────────────────────┬───────────────────────────────────┘│
│  ┌─ 缓存层 (自研) ─────────┴──────────────────────────────────┐│
│  │  LRU 10000条 + 术语库 + 人名/地名一致性                     ││
│  └──────────┬─────────────┬───────────────────────────────────┘│
│  ┌──────────▼──────────────▼──────────────────────────────────┐│
│  │  翻译模型层 (自研)                                          ││
│  │  用户自填第三方 API (密钥 + URL)                            ││
│  │  短文本: 小模型快速翻译    长文本: 大模型高质量翻译          ││
│  │  OpenAI 兼容协议 (后续可扩展)                               ││
│  └────────────────────────────────────────────────────────────┘│
│  ┌─ 富文本处理 (自研) ────────────────────────────────────────┐│
│  │  标记提取 -> 纯文本翻译 -> 标记回填                         ││
│  └────────────────────────────────────────────────────────────┘│
│  ┌─ OCR 兜底 (自研封装) ──────────────────────────────────────┐│
│  │  ONNX Runtime + DirectML + PaddleOCR 模型                  ││
│  │  变化检测 + 区域限制 + 15fps                               ││
│  └────────────────────────────────────────────────────────────┘│
└────────────────────────────────────────────────────────────────┘
┌────────────────────────────────────────────────────────────────┐
│  GUI (WPF, C# .NET 8)                                          │
│  进程选择器 / API配置(密钥+URL) / 翻译设置 / 快捷键            │
│  性能监控 / 术语管理                                           │
└────────────────────────────────────────────────────────────────┘
```

**核心原则：**
- 翻译进程独立运行，不抢游戏 CPU/GPU
- 共享内存通信，比 Socket/Pipe 快一个数量级
- 全异步链路，零阻塞游戏渲染管线
- 翻译走第三方 API，不占本地显卡资源

---

## 2. 编程语言选型

> 每个组件用最佳语言，不强行统一。

| 组件 | 语言 | 理由 |
|------|------|------|
| 翻译进程 + GUI | **C# (.NET 8)** | 与 XUnity.AutoTranslator/MelonLoader 同栈，WPF GUI (DirectX 硬件加速)，HTTP/JSON 原生支持 |
| Unity 插件 | **C#** | XUnity.AutoTranslator + MelonLoader 生态全是 C# |
| 注入DLL (非Unity游戏) | **C++ + MinHook** | Windows inline hook 生态最成熟，x86/x64，MIT 协议 |
| Frida 动态 Hook 脚本 | **JavaScript** | Frida 原生脚本语言，`Interceptor.attach` |
| Renpy Hook | **Python** | Renpy 本身就是 Python，monkey-patch 即可 |

---

## 3. 文本提取

### 3.1 Unity 引擎 — 直接用 XUnity.AutoTranslator

**最佳轮子，直接集成。** 不自己造 Hook。

```
项目: https://github.com/bbepis/XUnity.AutoTranslator
注入: MelonLoader (支持 Mono + IL2CPP)
Hook 的文本框架:
  - TextMeshPro (TMP_Text)     ← 最常用
  - UGUI (UnityEngine.UI.Text)
  - NGUI (UILabel)
  - TextMesh (UnityEngine.TextMesh)
  - IMGUI (OnGUI)
  - FairyGUI (GTextField)
  - Utage (视觉小说引擎)
```

**为什么选 MelonLoader 而非 BepInEx：**

| 框架 | Mono | IL2CPP | 稳定性 |
|------|------|--------|--------|
| BepInEx 5 | ✅ | ❌ | 稳定，但不支持 IL2CPP |
| BepInEx 6 | ✅ | ✅ BleedingEdge | 无稳定版 |
| **MelonLoader** | ✅ | ✅ | **稳定，首个支持 IL2CPP 的通用 Mod Loader** |

现在越来越多 Unity 游戏用 IL2CPP 编译（反作弊+性能），MelonLoader 是最佳选择。

**我们的工作**：基于 XUnity.AutoTranslator 二次开发，替换翻译后端为自研调度引擎 + 第三方 API。

### 3.2 Renpy 引擎 — 自研 Python Hook

**无成熟通用轮子，自己做。** 但不需要 C 扩展，直接 Python monkey-patch：

```python
# 通过 renpy.bootstrap 或 -l 参数注入
import renpy.display.text

_original_set_text = renpy.display.text.Text.set_text

def _hooked_set_text(self, value, *args, **kwargs):
    # 推入共享内存队列
    push_to_queue(value)
    # 查缓存，命中则替换
    translated = cache_lookup(value)
    if translated:
        value = translated
    return _original_set_text(self, value, *args, **kwargs)

renpy.display.text.Text.set_text = _hooked_set_text
```

### 3.3 RPGMaker MV/MZ — 自研 JS Hook

**无通用轮子，自己做。** RPGMaker MV/MZ 本质是 WebView + JavaScript：

```javascript
// 注入 JS 脚本，重写原型方法
const _original_drawText = Window_Base.prototype.drawText;
Window_Base.prototype.drawText = function(text, x, y, maxWidth, lineHeight, align) {
    pushToQueue(text);
    const translated = cacheLookup(text) || text;
    return _original_drawText.call(this, translated, x, y, maxWidth, lineHeight, align);
};
```

### 3.4 其他引擎 — Frida 统一 Hook + OCR 兜底

**Frida 是最佳动态 Hook 轮子，直接用：**

```javascript
// Frida JS 脚本，hook 任意引擎的文本渲染函数
Interceptor.attach(Module.findExportByName(null, 'target_func'), {
    onEnter(args) {
        const text = args[0].readUtf8String();
        pushToQueue(text);
        const translated = cacheLookup(text);
        if (translated) args[0].writeUtf8String(translated);
    }
});
```

**OCR 兜底**（无法 Hook 的引擎）：
1. 先用 OCR 识别，记录文本位置
2. 识别到足够多的文本后，映射到内存地址
3. 后续直接读内存，不再走 OCR

---

## 4. 翻译调度（自研核心）

### 4.1 共享内存设计

```c
// 无锁单生产者单消费者环形队列
// 使用 InterlockedCompareExchange + 内存屏障保证线程安全
struct SharedBuffer {
    // 生产者: GameHook 写入
    volatile uint32_t write_index;       // 写入位置 (原子写)
    char pending_texts[64][256];         // 环形缓冲区，64条待翻译
    uint32_t text_lengths[64];           // 每条文本长度
    uint64_t text_hashes[64];            // 每条文本 FNV-1a hash (去重)

    // 消费者: Translator.exe 写入
    volatile uint32_t read_index;        // 已读取位置 (原子写)
    char translated[64][256];            // 翻译结果
    volatile uint32_t translated_count;  // 可用结果数 (原子写)
};
```

**同步策略**：
- 单生产者(GameHook)单消费者(Translator)模型
- `write_index` / `read_index` / `translated_count` 使用 `InterlockedCompareExchange` 原子操作
- 读写前后加 `_ReadWriteBarrier()` / `MemoryBarrier()` 防止 CPU 乱序
- 满队时丢弃最旧文本（游戏文本时效性强，旧文本无意义）

### 4.2 翻译策略

```
1. 去重合并:   相同文本 hash 只翻译一次
2. 批量聚合:   100ms 窗口内的文本打包一次 API 请求
3. 优先级:     当前屏幕可见文本 > 后台文本
4. 超时降级:   200ms 无结果，先用原文显示，译文到了再刷新
```

### 4.3 LRU 缓存

```
容量: 最近 10000 条
键:   FNV-1a hash(原文 + 源语言 + 目标语言)
值:   译文
命中率预估: 重复文本(菜单、UI) >85%，对话文本 ~40%
存储: 内存 HashMap，可选持久化到 SQLite
```

### 4.4 术语库 + 一致性

```
术语库:   用户可编辑的术语映射表 (如角色名、地名)
一致性:   同一原文在整个游戏内翻译一致
         人名/地名首次出现时锁定翻译，后续引用直接使用
```

---

## 5. 翻译模型层（自研）

### 5.1 架构

```
用户自填第三方 API 配置:
  - API URL (如 https://api.openai.com/v1/chat/completions)
  - API Key
  - 模型名称
  - 协议格式 (默认 OpenAI 兼容，后续可扩展)

翻译分流:
  短文本 (UI/菜单/物品名, ≤50字):  快速请求，优先低延迟
  长文本 (剧情对话, >50字):        带上下文请求，优先翻译质量
  重复文本:                        LRU 缓存直接命中，不请求 API
```

### 5.2 API 调用

```csharp
// C# 翻译进程核心调用逻辑
public class TranslationService
{
    private readonly HttpClient _client;
    private readonly ApiConfig _config;  // 用户填入的 URL + Key + Model

    // 短文本: 单条快速翻译
    public async Task<string> TranslateShort(string text, string from, string to)
    {
        var cacheHit = _cache.Lookup(text, from, to);
        if (cacheHit != null) return cacheHit;

        var request = BuildRequest(text, from, to, _config);
        var response = await _client.PostAsync(_config.Url, request);
        var translated = ParseResponse(response);
        _cache.Store(text, translated, from, to);
        return translated;
    }

    // 长文本: 批量+上下文翻译
    public async Task<List<string>> TranslateBatch(
        List<string> texts, string from, string to, string context)
    {
        // 100ms 窗口聚合，带前文上下文一起发
        var request = BuildBatchRequest(texts, from, to, context, _config);
        var response = await _client.PostAsync(_config.Url, request);
        return ParseBatchResponse(response);
    }
}
```

### 5.3 Prompt 工程

```
系统 Prompt (游戏翻译专用):
  你是专业游戏翻译引擎。要求：
  1. 保持游戏语气和风格
  2. 人名/地名按术语表翻译
  3. 保留所有 {标记} 原样不翻译
  4. 译文长度尽量接近原文
  5. 输出纯译文，不要解释

上下文注入:
  将前 2-3 条已翻译文本作为 few-shot 示例
  保持剧情连贯性和术语一致性
```

---

## 6. 富文本处理（自研）

游戏文本常带内联标记，直接翻译会破坏格式：

```
原文: <color=#fff>NPC</color>: <shake>What?!</shake>
错误: 直接整段丢给 API -> 标记被打乱或丢失
正确: 提取纯文本 -> 翻译 -> 回填标记
```

### 处理流程

```
1. 标记提取:  用正则/解析器提取所有标记，替换为占位符
              "<color=#fff>NPC</color>: <shake>What?!</shake>"
              -> "{0}: {1}"  纯文本: ["NPC", "What?!"]

2. 翻译:     纯文本送 API 翻译
              ["NPC", "What?!"] -> ["NPC", "什么？！"]

3. 标记回填:  将译文填回占位符位置
              "{0}: {1}" -> "<color=#fff>NPC</color>: <shake>什么？！</shake>"
```

**支持的标记格式**：
- Unity Rich Text: `<color>`, `<size>`, `<b>`, `<i>`, `<quad>`
- Renpy Text Tags: `{color}`, `{size}`, `{b}`, `{i}`, `{w}`, `{p}`
- RPGMaker escape codes: `\C[n]`, `\V[n]`, `\N[n]`
- 通用 XML/HTML 风格标记

---

## 7. 文本注入（自研）

### 7.1 内存写回（首选，零渲染开销）

```
Hook setter -> 查缓存 -> 有翻译直接替换原文 -> 引擎渲染
原文: "こんにちは" -> 缓存命中 -> 替换为 "你好" -> 引擎渲染 "你好"
无额外渲染开销，完全透明
```

### 7.2 排版自适应（自研，解决溢出问题）

译文长度变化导致文本框溢出是**最常见的问题**：

```
原文: "Hello"          -> 译文: "你好，世界的朋友们"  (长度爆炸)
原文: "Yes"            -> 译文: "是的"                (长度合理)

自适应策略:
1. 文本框宽度自动扩展 (Hook 排版参数)
2. 自动换行 (插入 \n 到合适位置)
3. 字号缩放 (译文超长时自动缩小字号，最小不小于原文 70%)
4. 行距调整 (多行文本时压缩行距)
```

### 7.3 字体替换

```
预加载: NotoSansCJK-Regular.ttc + NotoSansCJK-Bold.ttc
        (思源黑体，SIL Open Font License，中日韩全覆盖)
注入时机: 游戏引擎初始化字体时
方式: Hook 字体加载函数，替换为内存中的中文字体
优点: 游戏引擎自己渲染，零额外 GPU 开销
```

### 7.4 透明窗口叠加（兜底）

```
仅对无法 Hook 的引擎使用
优化:
  - 只渲染变化的文本区域（脏矩形）
  - 硬件加速合成（DWM）
  - 不重建窗口，复用同一 HWND
```

---

## 8. OCR 兜底（自研封装）

### 8.1 方案

```
ONNX Runtime + DirectML (GPU) + PaddleOCR PP-OCRv5 模型
仅对无法 Hook 的引擎启动

为什么自研封装而非直接用 RapidOcrNet:
  - RapidOcrNet 不原生支持 DirectML（AMD/Intel GPU 不可用）
  - 自己控制 ONNX Runtime SessionOptions，可灵活配置 EP
  - 可自定义预处理/后处理管线，针对游戏文字优化
```

### 8.2 GPU 加速配置

```csharp
// 自研 OCR 引擎，ONNX Runtime DirectML
using var sessionOptions = new SessionOptions();
sessionOptions.GraphOptimizationLevel = GraphOptimizationLevel.ORT_ENABLE_EXTENDED;

// 优先 DirectML (支持 AMD/NVIDIA/Intel 全系 GPU)
try { sessionOptions.AppendExecutionProvider_DML(0); }
catch
{
    // 降级: CUDA (NVIDIA 专用)
    try { sessionOptions.AppendExecutionProvider_CUDA(0); }
    catch { sessionOptions.AppendExecutionProvider_CPU(); }
}
```

### 8.3 优化

```
1. 区域限制:   只扫描文本区域（对话框位置），不全屏扫描
2. 变化检测:   前后帧 diff，只处理变化的区域
3. 频率控制:   最高 60fps，实际可降到 15fps（文字变化不快）
4. 动态分辨率: 远处文字用半分辨率，近处用全分辨率
```

### 8.4 性能预估

```
CPU ONNX:        15-50ms/帧  (现有方案)
GPU DirectML:    2-5ms/帧    (优化后)
GPU CUDA:        1-3ms/帧    (NVIDIA 专属)
```

---

## 9. 技术栈总览

### 直接用（最佳轮子）

| 组件 | 选型 | 语言 | 协议 | 理由 |
|------|------|------|------|------|
| Unity 文本 Hook | XUnity.AutoTranslator | C# | MIT | 7种文本框架+IL2CPP，生态最成熟 |
| Unity 注入框架 | MelonLoader | C# | Apache 2.0 | 首个支持 IL2CPP 的稳定通用 Mod Loader |
| 动态 Hook(非Unity) | Frida | JS/C++ | 非标准 | 跨平台动态插桩，业界标准 |
| DLL Inline Hook | MinHook | C++ | MIT | x86/x64 最小化 hook 库 |
| 共享内存 | Windows MMF | - | 系统级 | 零拷贝，微秒级延迟 |
| 字体 | NotoSansCJK Regular+Bold | - | OFL | 思源黑体，中日韩全覆盖，免费商用 |
| GUI 框架 | WPF | C# | MIT | DirectX 硬件加速，Windows 原生，系统字体直接用，单文件部署 |
| OCR 模型 | PaddleOCR PP-OCRv5 | - | Apache 2.0 | 开源，高精度，多语言 |
| 推理引擎 | ONNX Runtime | C# | MIT | DirectML/CUDA/CPU 三路降级 |

### 自研（轮子不够最佳或没轮子）

| 组件 | 语言 | 理由 |
|------|------|------|
| 翻译调度引擎 | C# | 共享内存无锁队列+批量聚合+优先级+超时降级，无现成方案 |
| 翻译模型层 | C# | 第三方 API 集成+短长文本分流+Prompt 工程 |
| LRU 缓存 + 术语库 | C# | 缓存+一致性维护，自研可控 |
| 富文本处理 | C# | 标记提取->翻译->回填，支持多引擎标记格式 |
| 排版自适应 | C# | 文本框宽度/换行/字号缩放，解决译文溢出 |
| OCR 集成封装 | C# | ONNX Runtime+DirectML 自配，游戏文字优化 |
| Renpy Hook | Python | 无成熟通用方案，monkey-patch 实现 |
| RPGMaker Hook | JavaScript | 无通用方案，原型重写实现 |
| 透明窗口叠加 | C++ | DWM 硬件合成+脏矩形，无通用方案 |
| GUI | C# (WPF) | 进程选择器/API配置/翻译设置/性能监控 |

---

## 10. 性能预估

| 操作 | 耗时 | 对游戏 FPS 影响 |
|------|------|----------------|
| Hook 拦截文本 | <0.01ms | 无 |
| 共享内存写入 | <0.01ms | 无 |
| 缓存命中替换 | <0.05ms | 无 |
| 缓存未命中（异步） | 0ms（不阻塞） | 无 |
| 富文本提取/回填 | <0.05ms | 无 |
| 排版自适应 | <0.1ms | 无 |
| 字体替换初始化 | 一次性操作 | 仅加载时 |
| OCR 兜底 (GPU) | 2-5ms | 微小 |
| 透明窗口叠加 | 0.5-1ms | 微小 |
| 第三方 API 翻译 | 200-2000ms（异步） | 无（不阻塞渲染） |

**结论：引擎 Hook + 字体替换路径，每帧额外开销 < 0.2ms，对游戏 FPS 无影响。**

---

## 11. 实现步骤（MVP 优先）

### 第〇阶段：项目框架 ✅ 已完成

1. ✅ 目录结构搭建
2. ✅ 共享内存结构体定义 (SharedMemory.h)
3. ✅ 通信协议文档 (Protocol.md)
4. ✅ 技术方案定稿

### 第六阶段：GUI 基础 ✅ 已完成

> WPF + MVVM (CommunityToolkit.Mvvm)，5 个页面暗色主题，提前完成以便可视化验证

1. ✅ WPF GUI 框架搭建（无边框窗口 + 侧边栏导航）
2. ✅ 进程选择器 + API 配置界面（URL/Key/模型名）
3. ✅ 翻译设置（缓存/富文本/排版/OCR/术语开关）
4. ✅ 性能监控面板（8 项指标卡片 + 实时翻译流）
5. ✅ 术语管理（增删术语 + 分类标签）
6. ⬜ 快捷键（开关翻译、切换语言）

**GUI 开发与启动方式**：
```bash
# WSL2 交叉编译
cd src/gui
dotnet build                                              # 编译
dotnet publish -c Release -r win-x64 --self-contained true  # 发布自包含 exe

# Windows 侧运行
dotnet run           # 开发模式（需 Windows .NET 8 SDK）
# 或双击 publish/GameTranslator.exe（自包含，无需安装运行时）
```

### 第一阶段：Unity 核心链路（1周）⬜ 待开发

> 基于 XUnity.AutoTranslator，不从零写 Hook

1. Fork XUnity.AutoTranslator，理解其 Hook 架构
2. 替换翻译后端：接入自研调度引擎 + 第三方 API
3. 用户配置：API URL + Key + 模型名称
4. 跑通：Unity 游戏 -> Hook 文本 -> 缓存 -> API 翻译 -> 写回

### 第二阶段：翻译调度引擎（1周）⬜ 待开发

1. 实现共享内存无锁环形队列（C# + MMF）
2. 实现 LRU 缓存（10000条）
3. 实现批量聚合（100ms 窗口）
4. 实现异步回调 + 超时降级
5. 实现术语库 + 一致性维护

### 第三阶段：富文本 + 排版（1周）⬜ 待开发

1. 富文本标记提取/回填（Unity/Renpy/RPGMaker 格式）
2. 排版自适应（文本框宽度/换行/字号缩放）
3. 字体替换（NotoSansCJK 注入）

### 第四阶段：多引擎支持（2周）⬜ 待开发

1. Renpy Python Hook
2. RPGMaker JS Hook
3. Frida 统一 Hook 框架搭建

### 第五阶段：OCR 兜底（1周）⬜ 待开发

1. ONNX Runtime + DirectML 封装
2. PaddleOCR PP-OCRv5 模型集成
3. 变化检测 + 区域限制 + 频率控制

---

## 12. 与现有工具对比

| 维度 | LunaTranslator | XUnity.AutoTranslator | 本方案 |
|------|---------------|----------------------|--------|
| Unity 文本 Hook | OCR 为主 | ✅ 7种框架+IL2CPP | 基于 XUnity 二次开发 |
| IL2CPP 支持 | 未知 | ✅ | ✅ (MelonLoader) |
| Renpy Hook | ✅ HOOK | ❌ | ✅ Python monkey-patch |
| RPGMaker Hook | ✅ HOOK | ❌ | ✅ JS 重写 |
| 翻译后端 | 第三方 API | 第三方 API | 第三方 API (用户自填) |
| 翻译调度 | 逐条请求 | 逐条请求 | **批量聚合+优先级+超时降级** |
| 富文本处理 | 未知 | 正则(简单) | **标记提取->翻译->回填** |
| 排版自适应 | 无 | 无 | **宽度/换行/字号缩放** |
| 共享内存通信 | 无 | 无 | **MMF 无锁零拷贝** |
| LRU 缓存 | 有 | 有 | ✅ + 术语库+一致性 |
| OCR | ✅ 多引擎 | ❌ | ✅ DirectML GPU 加速 |
| 本地性能 | 够用 | 够用 | **<0.2ms/帧** |
| GUI | ✅ | ❌ 配置文件 | ✅ WPF (5页面暗色主题) |

**差异化壁垒**：
1. 翻译调度引擎（批量+优先级+超时降级）
2. 富文本处理（标记保留）
3. 排版自适应（解决溢出）
4. 共享内存零拷贝（极致性能）

---

## 13. 注意事项

- **勿捆绑盗版游戏分发**：工具本身合法，但配合盗版传播游走灰色地带
- **杀软误报**：DLL 注入行为会被杀软标记，需加白名单
- **游戏反作弊**：部分在线游戏有反注入检测，仅用于单机游戏
- **字体版权**：NotoSansCJK 是 SIL Open Font License，可商用
- **翻译结果缓存**：个人使用不涉及版权问题，不可将他人游戏的翻译文本公开发布
- **API 密钥安全**：用户密钥仅存储在本地配置，不上传任何服务器
