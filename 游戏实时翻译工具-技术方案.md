# Game Translator 0.1.0 技术方案

本文记录 `0.1.0` 的实际实现，不作为未来功能路线图。当前版本已在两款 Unity Mono x64 游戏中完成真实翻译闭环测试，后续以维护和问题修复为主。

## 1. 产品边界

### 1.1 已支持

- Windows 10/11 x64。
- 无反作弊组件的 Unity Mono x64 单机游戏。
- 通过 BepInEx 和 XUnity.AutoTranslator 提取、翻译并回填文本。
- 英语、日语翻译为简体中文。
- 用户提供 OpenAI Chat Completions 兼容接口、API Key 和模型名称。
- 单条翻译、批量翻译、内存缓存、SQLite 持久缓存和运行指标。
- 自动下载、校验并安装 BepInEx、XUnity.AutoTranslator 和匹配字体。

### 1.2 不支持

- Unity IL2CPP、32 位游戏及非 Unity 引擎。
- 含反作弊组件或在线竞技保护的游戏。
- OCR、屏幕叠加层、原生 Hook 和跨平台运行。
- 保证兼容所有 Unity 版本、Loader 或游戏自定义文本框架。

## 2. 总体架构

```text
┌──────────────────── Unity 游戏进程 ────────────────────┐
│ BepInEx                                                │
│ └─ XUnity.AutoTranslator                               │
│    └─ CustomTranslate.dll                              │
│       ├─ 收集单条或批量文本                            │
│       └─ POST Base64 文本数组                          │
└────────────────────────┬───────────────────────────────┘
                         │ 127.0.0.1:52731
                         │ 随机路径令牌 + 游戏身份
┌────────────────────────▼───────────────────────────────┐
│ GameTranslator.exe（.NET 10 / WPF）                    │
│ ├─ 配置、启动、测试和性能监控界面                      │
│ ├─ XUnity 本机 HTTP 桥接                               │
│ └─ TranslationRuntime                                  │
│    ├─ 有界队列与同请求合并                             │
│    ├─ 内存缓存                                         │
│    ├─ SQLite 持久缓存                                  │
│    └─ OpenAI Chat Completions 兼容客户端               │
└────────────────────────┬───────────────────────────────┘
                         │ HTTPS
                         ▼
                 用户配置的模型服务
```

翻译运行时直接置于 GUI 进程中，不增加独立服务、依赖注入容器或 Provider 工厂。发布文件更少，启动和故障排查也更直接。

## 3. 技术栈

| 组件 | 实现 |
|------|------|
| 桌面应用 | C#、.NET 10、WPF、CommunityToolkit.Mvvm |
| Unity Loader | BepInEx 5.4.23.5 |
| 文本提取与回填 | XUnity.AutoTranslator 5.6.1 |
| 批量端点 | C#、XUnity `HttpEndpoint` |
| 本机桥接 | `TcpListener`、回环 HTTP |
| API 客户端 | 单例 `HttpClient`、`System.Text.Json` |
| 异步队列 | `System.Threading.Channels` |
| 持久缓存 | SQLite、Microsoft.Data.Sqlite |
| 密钥保护 | Windows DPAPI CurrentUser |
| 构建发布 | .NET SDK、Bash、7-Zip |

## 4. 游戏接入与启动

用户选择或拖入游戏 EXE 后，程序执行以下流程：

1. 检查游戏目录、`UnityPlayer.dll` 和 Mono `Managed` 目录。
2. 按 SHA-256 校验并安装 BepInEx 与 XUnity.AutoTranslator。
3. 安装发布程序内嵌的 `CustomTranslate.dll`；该 DLL 在发布阶段从当前源码重建。
4. 根据 Unity 主版本选择并安装字体资源。
5. 更新 `AutoTranslatorConfig.ini`：
   - `Endpoint=CustomTranslate`；
   - 清空 Fallback Endpoint；
   - 写入本机桥接 URL；
   - 写入源语言、简体中文目标语言和字体；
   - 启用 XUnity 批处理。
6. 启动本机桥接，再直接启动用户选择的游戏 EXE。
7. 保存游戏路径并记录实际进程 PID，软件重启后可恢复运行状态判断。

XUnity 文件缓存保存到 `Cache\Games\{稳定游戏身份}\{Lang}\Text\`，避免同名游戏或不同目录的游戏共用译文。

## 5. 本机桥接协议

桥接仅监听：

```text
127.0.0.1:52731
```

实际路径包含首次生成的 128 位随机令牌和稳定游戏身份：

```text
POST /translate/{随机令牌}/{游戏身份}?from=ja&to=zh
Content-Type: text/plain; charset=utf-8
```

请求正文为逐行 Base64 编码的 UTF-8 文本，响应按相同顺序返回逐行 Base64 译文。单条请求也走同一批量协议。

协议限制：

- 每批 1～100 条；
- 单条文本最多 20,000 个字符；
- 批量正文最多 512 KiB；
- 请求头最多 64 KiB；
- 最多同时处理 32 个本机连接；
- 单连接只处理一个请求并返回 `Connection: close`；
- 读取或翻译总超时最长 120 秒。

一连接一请求避免自行实现完整 HTTP keep-alive 缓冲状态机，也不会影响 XUnity 的批量能力。连接槽耗尽时返回 HTTP 503。

## 6. 翻译运行时

### 6.1 单条流程

```text
校验与规范化
  → 构建缓存身份和 SHA-256 索引键
  → 内存缓存
  → SQLite 缓存
  → 合并相同的进行中请求
  → 有界队列
  → OpenAI 兼容 API
  → SQLite 与内存缓存
  → 返回译文
```

相同缓存身份的并发请求共享一个 API 调用。每个调用者独立取消；最后一个等待者离开后才取消底层请求。

### 6.2 批量流程

```text
校验 1～100 条文本
  → 一次扫描内存缓存
  → 一次 SQLite 批量查询
  → 将全部未命中文本放入一个模型请求
  → 校验译文数组数量、顺序和空值
  → 逐条写入缓存
  → 按原顺序返回
```

模型输入和输出均为 JSON 字符串数组。XUnity 是否形成多条批次取决于同一帧内的待翻译队列；实时逐条出现的字幕可能仍以单条批次发送。

### 6.3 并发与背压

- 有界 Channel 容量为 128。
- 固定启动 2 个翻译 Worker。
- 单条相同请求会合并；批量请求由 XUnity 负责聚合。
- 队列、文本、请求正文、响应正文、连接数和缓存均设上限。
- API 响应正文最多 1 MiB。

## 7. OpenAI 兼容接口

Base URL 会规范化为：

```text
{Base URL}/chat/completions
```

认证使用：

```text
Authorization: Bearer {API Key}
```

请求包含 `system`、`user` 和 `model`。默认发送 `thinking: { type: "disabled" }`；若服务端返回 HTTP 400，则去掉该字段重试，并记住该接口与模型组合只使用标准字段。

重试规则：

- 网络错误、HTTP 429 和 5xx 最多尝试 3 次；
- 优先遵守 `Retry-After`；
- 否则使用 250、500 毫秒递增等待；
- 认证错误、额度错误及其他不可恢复的 4xx 不重试；
- 超时和用户取消通过 `CancellationToken` 传递。

安全限制：Base URL 必须使用 HTTPS，仅回环地址允许 HTTP；URL 不允许携带查询参数或片段。

## 8. Prompt 与输出

当前 Prompt 版本为 `2`，要求模型：

- 只输出译文，不解释或回答原文问题；
- 保留换行、转义符、数字、占位符和富文本标签；
- 将短词按游戏界面语境处理。

单条响应必须包含非空 `choices[0].message.content`。批量响应必须能解析为与输入数量相同、顺序一致且不含空白项的字符串数组，否则本次请求失败，不写入缓存。

## 9. 缓存设计

### 9.1 缓存层级

```text
L1：进程内字典，约 32 MiB
L2：SQLite，文本载荷约 224 MiB，数据库硬上限 256 MiB
```

L1 使用简单有界策略；写入将超出预算时整体清空。该策略代码少、常数开销低，当前没有命中率或锁竞争数据证明需要严格 LRU。

L2 使用 WAL 模式，Schema 版本为 `4`。每次访问更新 `last_hit_utc`；超过文本载荷预算时按最久未命中顺序淘汰。批量翻译使用一次 SQLite 查询读取全部候选缓存。

### 9.2 缓存身份

缓存身份包含：

- 完整规范化原文；
- 稳定游戏身份；
- 源语言与目标语言；
- Base URL；
- 模型名称；
- Prompt 版本。

字段采用长度前缀编码，避免分隔符碰撞；再计算 SHA-256 作为 SQLite 主键。数据库同时保存并核对完整身份，即使 Hash 碰撞也不会返回错误译文。

### 9.3 清理与迁移

- 清缓存通过写入门和缓存代际同步，防止已开始的后台请求在清理后恢复旧缓存。
- 同时清除进程内缓存、SQLite 缓存和各游戏的 XUnity 文件缓存。
- 运行中的游戏可能持有 XUnity 内存缓存，因此界面要求先关闭游戏。
- 旧 SQLite 主库、WAL 和 SHM 作为文件组迁移，避免拆分或错配。
- 更高 Schema 版本会被拒绝；不可恢复的数据库会先备份，再创建新库。

## 10. 配置与安全

- 普通配置位于软件目录 `Data\config.json`。
- API Key 使用 Windows DPAPI `CurrentUser` 加密后写入配置，不保存明文副本。
- 配置采用临时文件替换方式原子写入。
- 缓存位于软件目录 `Cache\`，不包含 API Key。
- 崩溃日志位于 `Data\crash.log`。
- API Key 不写入翻译日志、SQLite 或 XUnity 配置。
- 本机桥接随机令牌保存在 `Data\bridge-token.txt`。
- 桥接只监听回环地址，并校验随机令牌、游戏身份和请求大小。

## 11. 运行指标

软件每秒刷新一次指标：

- 最近一次翻译延迟；
- 缓存命中率；
- 当前队列长度；
- 已完成翻译数量；
- 目标游戏进程 CPU 使用率；
- 目标游戏进程工作集内存。

CPU 与内存数值来自目标游戏进程，不是模型服务端或整机总占用。实际资源和延迟取决于游戏、文本量、缓存命中率、网络和模型服务，不提供脱离测试环境的固定性能承诺。

## 12. 故障处理

| 场景 | 行为 |
|------|------|
| API 认证失败 | 配置页显示 HTTP 状态码，不保存错误配置 |
| API 超时或限流 | 有限重试后失败，由 XUnity 保留或再次请求原文 |
| API 返回非法 JSON | 拒绝结果，不写缓存 |
| 批量译文数量不一致 | 拒绝整批，不产生顺序错配 |
| 桥接空闲读取超时 | 静默关闭连接，不发送无请求对应的响应 |
| 桥接连接槽耗尽 | 返回 HTTP 503 |
| 数据库迁移失败 | 备份旧库并显示恢复警告 |
| 清缓存时仍有写入 | 通过写入门和代际校验阻止缓存复活 |
| 游戏先退出 | 停止进程采样，软件继续运行 |
| GUI 先退出 | 本机桥接终止，XUnity 不再取得新译文 |

## 13. 构建与发布

`tools/publish.sh` 执行以下步骤：

1. 下载 XUnity.AutoTranslator 5.6.1 发布包。
2. 校验固定 SHA-256。
3. 使用发布包内的核心程序集从源码构建 `CustomTranslate.dll`。
4. 将 DLL 嵌入 GUI 程序，避免源码与二进制漂移。
5. 发布 Windows x64 自包含 Release 和 Debug 程序。

```bash
bash tools/publish.sh [输出目录]
```

自包含发布不要求用户安装 .NET Runtime，但文件体积和程序工作集会高于依赖框架发布。

## 14. 验证状态

### 14.1 自动检查

`tests/RuntimeChecks` 覆盖：

- 语言、配置和 URL 边界；
- API 认证头、请求格式、重试、取消及响应上限；
- `thinking` 字段兼容降级；
- 单条和批量译文顺序校验；
- 批量正文上限；
- 缓存身份游戏隔离、字段边界和 Hash 碰撞防护；
- SQLite 读写、迁移、淘汰、清理和更高版本拒绝；
- DPAPI 密钥加密保存；
- XUnity 配置、字体选择和嵌入资源；
- 性能监控页面加载及 CPU 计算。

RuntimeChecks 目标为 Windows，Linux/WSL2 可编译，但缺少 `Microsoft.WindowsDesktop.App` 时不能直接运行。

### 14.2 实际游戏测试

`0.1.0` 已在两款 Unity Mono x64 游戏中验证：

- 游戏选择与直接启动正常；
- BepInEx、XUnity 和字体安装正常；
- API 认证与真实模型调用正常；
- 单条与 100 条批量请求均有成功记录；
- 文本提取、译文回填和持久缓存正常；
- 不同游戏缓存相互隔离。

具体游戏名称和硬件数据未纳入仓库，因此本文不声明兼容矩阵或固定 CPU、内存与延迟数字。

## 15. 当前结论

`0.1.0` 已完成预定的 Unity Mono 实时翻译闭环。当前实现继续采用 WPF 单进程宿主、XUnity 官方扩展点、本机回环 HTTP、OpenAI 兼容 API 和两级缓存，不引入独立 Worker、Named Pipe、共享内存、原生 Hook、OCR 或其他游戏引擎适配。

后续暂无功能扩展计划，仅在出现可复现问题或新的明确需求时维护。

## 16. 参考资料

- [XUnity.AutoTranslator](https://github.com/bbepis/XUnity.AutoTranslator#readme)
- [.NET WPF 文档](https://learn.microsoft.com/dotnet/desktop/wpf/overview/)
- [.NET HttpClient 文档](https://learn.microsoft.com/dotnet/fundamentals/networking/http/httpclient-guidelines)
- [System.Threading.Channels](https://learn.microsoft.com/dotnet/core/extensions/channels)
- [Microsoft.Data.Sqlite](https://learn.microsoft.com/dotnet/standard/data/sqlite/)
- [SQLite 文档](https://www.sqlite.org/docs.html)
