# 游戏实时翻译工具 — 最佳性能技术方案

## 1. 总体架构

```
┌─────────────────────────────────────────────────────┐
│  游戏进程 (Game.exe)                                  │
│  ┌──────────────────────────────────────────────┐   │
│  │  注入DLL (GameHook.dll)                       │   │
│  │  ├─ 文本HOOK: 拦截引擎渲染函数                  │   │
│  │  ├─ 字体替换: 注入中文字体                       │   │
│  │  └─ 共享内存: 读写翻译数据                       │   │
│  └──────────────────────────────────────────────┘   │
│                         │ 共享内存 (零拷贝)            │
│  ┌──────────────────────▼────────────────────────┐   │
│  │  翻译进程 (Translator.exe)                      │   │
│  │  ├─ 文本队列: 100ms批量聚合                     │   │
│  │  ├─ LRU缓存: 已翻译文本命中率>70%               │   │
│  │  ├─ 异步HTTP: 调用你的模型API                   │   │
│  │  └─ OCR兜底: GPU加速 (未知引擎用)               │   │
│  └─────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

**核心原则：**
- 翻译进程独立运行，不抢游戏CPU/GPU
- 共享内存通信，比Socket/Pipe快一个数量级
- 全异步链路，零阻塞游戏渲染管线

---

## 2. 文本提取（引擎HOOK，首选方案）

### 2.1 Renpy 引擎
```
Hook: renpy.display.text.Text.set_text()  (Python层)
方式: 注入CPython扩展，替换成员函数
位置: 游戏目录/game/ 下所有 .rpyc 编译后的函数
```

### 2.2 Unity 引擎
```
Hook: TMPro.TMP_Text.text (setter)  (C#层)
方式: BepInEx + Harmony，HarmonyPatch
代码:
  [HarmonyPatch(typeof(TMP_Text), "set_text")]
  class TextHook {
      static void Prefix(ref string value) {
          // value 就是原文，推入共享内存队列
          PushToQueue(value);
          // 查缓存，有则替换
          var hit = CacheLookup(value);
          if (hit != null) value = hit;
      }
  }
```

### 2.3 RPGMaker MV/MZ
```
Hook: Window_Base.prototype.drawText()  (JS层)
方式: 注入JS脚本，重写原型方法
```

### 2.4 其他引擎（兜底）
- 先用OCR识别，记录文本位置
- 识别到足够多的文本后，映射到内存地址
- 后续直接读内存，不再走OCR

---

## 3. 翻译调度（关键性能层）

### 3.1 共享内存设计
```
struct SharedBuffer {
    // 生产者: GameHook.dll 写入
    uint32_t write_index;       // 写入位置
    char pending_texts[64][256]; // 环形缓冲区，64条待翻译
    uint32_t text_lengths[64];   // 每条文本长度
    uint64_t text_hashes[64];    // 每条文本hash (用于去重)

    // 消费者: Translator.exe 写入
    uint32_t read_index;         // 已读取位置
    char translated[64][256];    // 翻译结果
    uint32_t translated_count;   // 可用结果数
};
```

### 3.2 翻译策略
```
1. 去重合并: 相同文本hash只翻译一次
2. 批量聚合: 100ms窗口内的文本打包一次API请求
3. 优先级: 当前屏幕可见文本 > 后台文本
4. 超时降级: 200ms无结果，先用原文+透明叠加显示
```

### 3.3 LRU缓存
```
容量: 最近10000条
键: FNV-1a hash(原文 + 源语言 + 目标语言)
值: 译文
命中率预估: 重复文本(菜单、UI)>85%，对话文本~40%
```

---

## 4. 文本注入（零渲染开销）

### 4.1 字体替换（首选）
```
预加载: NotoSansSC-Bold.ttf (含中日韩全集)
注入时机: 游戏引擎初始化字体时
方式: Hook字体加载函数，替换为内存中的中文字体
优点: 游戏引擎自己渲染，零额外GPU开销
```

### 4.2 内存写回（HOOK到文本时可直接替换）
```
Hook setter → 查缓存 → 有翻译直接替换原文 → 引擎渲染
原文: "こんにちは" → 缓存命中 → 替换为 "你好" → 引擎渲染"你好"
无额外渲染开销，完全透明
```

### 4.3 透明窗口叠加（HOOK不可用时的兜底）
```
仅对未知/加密引擎使用
优化: 
  - 只渲染变化的文本区域（脏矩形）
  - 硬件加速合成（DWM）
  - 不重建窗口，复用同一HWND
```

---

## 5. OCR兜底（GPU加速）

### 5.1 方案
```
PaddleOCR-json + ONNX Runtime + DirectML (GPU)
仅对无法HOOK的引擎启动
```

### 5.2 优化
```
1. 区域限制: 只扫描文本区域（对话框位置），不全屏扫描
2. 变化检测: 前后帧diff，只处理变化的区域
3. 频率控制: 最高60fps，实际可降到15fps（文字变化不快）
4. 动态分辨率: 远处文字用半分辨率，近处用全分辨率
```

### 5.3 GPU加速对比
```
CPU ONNX: 15-50ms/帧  (现有方案)
GPU DirectML: 2-5ms/帧 (优化后)
```

---

## 6. 性能预估

| 操作 | 耗时 | 对游戏FPS影响 |
|------|------|-------------|
| HOOK拦截文本 | <0.01ms | 无 |
| 共享内存写入 | <0.01ms | 无 |
| 缓存命中替换 | <0.05ms | 无 |
| 缓存未命中（异步） | 0ms（不阻塞） | 无 |
| 字体替换初始化 | 一次性操作 | 仅加载时 |
| OCR兜底(GPU) | 2-5ms | 微小 |
| 透明窗口叠加 | 0.5-1ms | 微小 |

**结论：引擎HOOK+字体替换路径，每帧额外开销 < 0.1ms，对游戏FPS无影响。**

---

## 7. 技术栈选型

| 组件 | 选型 | 理由 |
|------|------|------|
| 注入DLL | C++ / Rust | 体积小，无运行时依赖 |
| 翻译进程 | C# / .NET 8 | 开发快，HTTP/JSON原生支持 |
| 共享内存 | Windows Memory Mapped File | 零拷贝，微秒级延迟 |
| OCR引擎 | PaddleOCR-json + DirectML | 开源，GPU加速 |
| 字体 | NotoSansSC-Bold | 免费商用，中日韩全覆盖 |
| 注入框架(Unity) | BepInEx + Harmony | 成熟稳定，MIT协议 |
| 注入框架(Renpy) | 自研 Python C扩展 | 轻量，无现成方案 |
| 自研模型API | HTTP/SSE流式 | 调用你自己的模型服务 |

---

## 8. 实现步骤（MVP优先）

### 第一阶段：核心链路（1-2周）
1. 写一个Unity游戏测试Demo
2. 实现BepInEx + Harmony Hook TMP_Text.text
3. 实现共享内存通信
4. 实现字体替换注入
5. 调通 原文→缓存→翻译→替换 的完整链路

### 第二阶段：翻译后端（1周）
1. 接入你的模型API
2. 实现LRU缓存
3. 实现批量聚合（100ms窗口）
4. 实现异步回调+超时降级

### 第三阶段：多引擎支持（按需）
1. Renpy引擎HOOK
2. RPGMaker引擎HOOK
3. OCR兜底方案

### 第四阶段：GUI（1周）
1. 进程选择器
2. 翻译设置（源语言/目标语言）
3. 快捷键（开关翻译、切换语言）
4. 性能监控面板

---

## 9. 与现有工具对比

| 维度 | 现有工具(RenpyThief) | 本方案 |
|------|---------------------|--------|
| 文本提取 | 主要靠OCR | 引擎HOOK优先，OCR兜底 |
| 翻译API | 付费GPT | 你的自建模型 |
| 翻译成本 | 按量收费 | 你自己的模型成本 |
| 文本注入 | 透明窗口叠加 | 字体替换+内存写回 |
| 本地性能 | 15-50ms/帧 | <0.1ms/帧 |
| 异步翻译 | 有(gptRealTime) | 全异步+批量聚合 |
| 缓存 | 未知 | LRU 10000条 |
| 多引擎 | Renpy/Unity/RPGMaker | 同，扩展性更好 |

---

## 10. 注意事项

- **勿捆绑盗版游戏分发**：工具本身合法，但配合盗版传播游走灰色地带
- **杀软误报**：DLL注入行为会被杀软标记，需加白名单
- **游戏反作弊**：部分在线游戏有反注入检测，仅用于单机游戏
- **字体版权**：NotoSansSC是SIL Open Font License，可商用
- **翻译结果缓存**：个人使用不涉及版权问题，不可将他人游戏的翻译文本公开发布